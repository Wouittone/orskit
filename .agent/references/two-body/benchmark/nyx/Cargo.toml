[package]
name = "nyx-two-body-benchmark"
version = "0.0.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[workspace]

[dependencies]
anise = { version = "=0.9.6", default-features = false }
hyperdual = "=1.4.0"
nyx-space = { version = "=2.3.1", default-features = false }
