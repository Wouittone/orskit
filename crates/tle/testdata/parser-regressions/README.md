# TLE parser regressions

Place minimized parser inputs here after a bounded parser-quality run finds a
panic or incorrect acceptance. Keep the exact bytes and add a focused assertion
to `tests/parser_regressions.rs` when the expected typed error is stable.
Markdown files are ignored by the regression runner.
